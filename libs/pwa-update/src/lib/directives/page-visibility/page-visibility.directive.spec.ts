import { PageVisibilityDirective } from './page-visibility.directive';

describe('PageVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new PageVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
